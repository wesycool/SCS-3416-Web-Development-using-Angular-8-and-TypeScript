<!DOCTYPE html>
<html>
<head>
    <title></title>
    <meta charset="utf-8" />
</head>
<body>
 
    <p>
        Say hello to
        <input id="name" type="text" />
        <button>Say it!</button>
    </p>
    <div id="content"></div>
    <script>
        /* Discuss unobstrusive event binding where
        we don't put the event and function in the button tag*/
    function sayHello() {
        console.log("this object:", this); //this object:<button>say it!</button>
        var name =
         document.getElementById("name").value;
        var message = "<h2>Hello " + name + "!</h2>";


        document
          .getElementById("content")
          .innerHTML = message;

    }

        //Unobstrusive event binding
        //The reason it is call this way is that the html doesn't need to know
        //anything about the javascript
        //document.querySelector looking for button.
        //the addEventListener listen for the "click" event and when that happens
        //I want you to use this function expression or value "sayHello"
        //this object:<button>say it!</button>
    document.querySelector("button").addEventListener("click", sayHello);
//	sayHello();
    </script>
</body>
</html>
